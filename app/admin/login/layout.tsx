import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Admin Sign In | Salt & Light Tool Rental",
};

export default function AdminLoginLayout({ children }: { children: React.ReactNode }) {
  return children;
}
